# Optional overlay when a host-level Caddy already owns ports 80/443.
# Start with: docker compose -f docker-compose.yml -f compose.external-caddy.yml up -d --build
services:
  app:
    ports:
      - '127.0.0.1:${APP_HOST_PORT:-3101}:3001'
  caddy:
    profiles: ['embedded-caddy']
