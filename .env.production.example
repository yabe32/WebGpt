# Ubuntu production with an already running host-level Caddy.
# Copy to .env on the server. Do not commit the resulting .env file.
DOMAIN=webgbt.duckdns.org
ACME_EMAIL=yabegaming2@gmail.com
APP_HOST_PORT=3101

# Optional: leave empty to use the Codex catalog default.
MODEL=
MAX_UPLOAD_MB=15
TURN_IDLE_TIMEOUT_SECONDS=180
